{
  "_doc": "Endpoints whose generated test should skip with a reason. Smoke keys: 'METHOD /api/...' or '/api/...' (both methods). Write-negative keys are namespaced 'NOTFOUND METHOD /api/...' / 'VALIDATION METHOD /api/...' so a skipped positive never hides a negative. Every entry is a finding, not a pardon: remove it when the handler is fixed.",
  "endpoints": {
    "NOTFOUND DELETE /api/access-groups/{group_id}/members/{user_id}": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/camp/camps/{camp_id}/team/{id}": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/documents/outputs/{id}/signatures/{sig_id}": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/insurance/prior-auths/{pa_id}/documents/{doc_id}": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/ipd/admissions/{id}/attenders/{aid}": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/lms/paths/{path_id}/courses/{course_id}": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/multi-hospital/hospital-assignments/{tenant_id}": "answers 204 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/multi-hospital/rotations/{id}": "answers 204 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/multi-hospital/user-assignments/{assignment_id}": "answers 204 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/opd/prescription-templates/{id}": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/pharmacy/staff/{id}": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/sensitive-patients/{id}": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND DELETE /api/setup/locations/{id}/staff/{user_id}": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND PATCH /api/dashboards/my/widgets/{wid}/visibility": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND PATCH /api/patients/{patient_id}/photo": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND POST /api/admin/simulator/runs/{id}/approve": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND POST /api/admin/simulator/runs/{id}/reject": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND POST /api/bedside/{admission_id}/feedback": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND POST /api/care-view/tasks/{task_id}/complete": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND POST /api/case-sheets/scans/{id}/submit": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND POST /api/chronic-care/patients/{patient_id}/check-interactions": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND POST /api/cms/posts/{id}/archive": "handler is not implemented (501)",
    "NOTFOUND POST /api/cms/posts/{id}/medical-review": "handler is not implemented (501)",
    "NOTFOUND POST /api/cms/posts/{id}/schedule": "handler is not implemented (501)",
    "NOTFOUND POST /api/command-center/alerts/{id}/acknowledge": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND POST /api/home-health/packages/{id}/consume": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND POST /api/hr/payroll/runs/{id}/finalize": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND POST /api/ipd/admissions/{id}/clinical-docs/{doc_id}/resolve": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND POST /api/ipd/admissions/{id}/discharge-summary/finalize": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND POST /api/notifications/{id}/read": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND POST /api/opd/encounters/{id}/verify-link": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND POST /api/pharmacy/orders/{id}/validate": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND POST /api/pharmacy/transfers/{id}/dispatch": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND POST /api/pharmacy/transfers/{id}/receive": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND POST /api/public/cms/posts/{post_id}/view": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND POST /api/utilization-review/reviews/{id}/ai-extract": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND PUT /api/abdm/gateway/callbacks/{id}/ack": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND PUT /api/camp/lab-samples/{id}/link": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND PUT /api/case-sheets/scans/{id}/review": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND PUT /api/command-center/transport/{id}/assign": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND PUT /api/command-center/transport/{id}/complete": "answers 200 for a record that does not exist — the write reports success without checking a row was affected",
    "NOTFOUND PUT /api/document-ingestion/items/{item_id}/ocr": "needs object storage, which the dev stack does not configure",
    "NOTFOUND PUT /api/indent/requisitions/{id}/cancel": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND PUT /api/indent/requisitions/{id}/reject": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND PUT /api/indent/requisitions/{id}/submit": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND PUT /api/lab/qc-results/{id}/review": "the reviewer self-check runs before the lookup, so an absent run answers 400",
    "NOTFOUND PUT /api/procurement/grns/{id}/complete": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND PUT /api/procurement/purchase-orders/{id}/cancel": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404",
    "NOTFOUND PUT /api/procurement/purchase-orders/{id}/send": "answers 400 'not found or wrong state' for a record that does not exist — existence and state are one conditional update; absence must answer 404"
  }
}

[package]
name = "medbrains-integration-hub"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "One integration facade: config-driven provider resolution + dual-provider routing + format normalization"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

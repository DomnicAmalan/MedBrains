//! Print-data crate — read-only endpoints that assemble print/PDF payloads.
//! Foundation-only coupling (server-core + medbrains-core::print_data types).

pub mod admin;
pub mod mrd;
pub mod regulatory;

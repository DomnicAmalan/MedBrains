apiVersion: v1
kind: Service
metadata:
  name: {{ include "medbrains-server.fullname" . }}
  labels:
    {{- include "medbrains-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "medbrains-server.selectorLabels" . | nindent 4 }}

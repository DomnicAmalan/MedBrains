{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "medbrains-server.serviceAccountName" . }}
  labels:
    {{- include "medbrains-server.labels" . | nindent 4 }}
{{- end }}

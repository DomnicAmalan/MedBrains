# `admin/paired-devices.tsx` walkthrough

_Source: [`apps/web/src/pages/admin/paired-devices.tsx`](../../../apps/web/src/pages/admin/paired-devices.tsx) (290 lines). Guard: `P.DEVICES.PAIRING.PAIRED_LIST`. API methods: 3. useForm: 0. Tables: 1. Modals: 1._

Tick each box that **works as expected** when you walk the page in a browser. Anything you can't tick → file in `../issues.md` with the line number, then fix and come back to tick it. Severity rubric: **P0** blocks core flow, **P1** broken UX with workaround, **P2** cosmetic.

## Page-level

- [ ] Page renders without `console.error`
- [ ] Network tab shows no 4xx/5xx on initial load
- [ ] Desktop viewport has no overlapping text, clipped buttons, or broken table layout
- [ ] Mobile/tablet viewport has no overlapping text, clipped buttons, or unusable controls
- [ ] Permission guard `P.DEVICES.PAIRING.PAIRED_LIST` redirects unauthorised user to /dashboard
- [ ] Loading skeleton / spinner shown while data loads
- [ ] Empty state visible when there are zero rows
- [ ] Page title in browser tab is correct
- [ ] Breadcrumb / nav highlights this page

## Tables / lists

### `<Table>` @ line 103
  - [ ] Header **Label** column shows correct value for at least one row
  - [ ] Header **Variant** column shows correct value for at least one row
  - [ ] Header **Cert fingerprint** column shows correct value for at least one row
  - [ ] Header **Paired at** column shows correct value for at least one row
  - [ ] Header **Last seen** column shows correct value for at least one row
  - [ ] Header **Status** column shows correct value for at least one row
  - [ ] Sortable column actually sorts (if interactive)
  - [ ] Pagination / load-more works (if applicable)

## Modals / Drawers

### Modal — _Mint pairing token_ @ [line 172](../../../apps/web/src/pages/admin/paired-devices.tsx#L172)
- [ ] Opens on trigger
- [ ] Required-field validation fires when fields blank
- [ ] Submit returns 2xx and shows success toast
- [ ] Closes after success
- [ ] List refetches and shows the new row
- [ ] Cancel button discards changes without warning if untouched, with confirm if dirty
- [ ] Server-side validation errors surface inline at the field, not just a banner

## Form inputs (3)

- [ ] **Device label** (`TextInput`, [line 215](../../../apps/web/src/pages/admin/paired-devices.tsx#L215)) — accepts input, default value sensible, persists after refresh
- [ ] **App variant** (`Select`, [line 222](../../../apps/web/src/pages/admin/paired-devices.tsx#L222)) — accepts input, default value sensible, persists after refresh
- [ ] **Notes** (`Textarea`, [line 233](../../../apps/web/src/pages/admin/paired-devices.tsx#L233)) — accepts input, default value sensible, persists after refresh

## Buttons / actions (`<Button>`: 4, `<ActionIcon>`: 0, `<Menu.Item>`: 0)

- [ ] **Mint pairing token** ([line 95](../../../apps/web/src/pages/admin/paired-devices.tsx#L95)) — click path works: disabled state correct, expected API/nav/modal/toast fires, and final row/status/value visibly changes
- [ ] **Mint pairing token** ([line 95](../../../apps/web/src/pages/admin/paired-devices.tsx#L95)) — failure path works: validation/server error is shown clearly and does not leave stale loading state
- [ ] **<button @ line 152>** ([line 152](../../../apps/web/src/pages/admin/paired-devices.tsx#L152)) — click path works: disabled state correct, expected API/nav/modal/toast fires, and final row/status/value visibly changes
- [ ] **<button @ line 152>** ([line 152](../../../apps/web/src/pages/admin/paired-devices.tsx#L152)) — failure path works: validation/server error is shown clearly and does not leave stale loading state
- [ ] **<button @ line 241>** ([line 241](../../../apps/web/src/pages/admin/paired-devices.tsx#L241)) — click path works: disabled state correct, expected API/nav/modal/toast fires, and final row/status/value visibly changes
- [ ] **<button @ line 241>** ([line 241](../../../apps/web/src/pages/admin/paired-devices.tsx#L241)) — failure path works: validation/server error is shown clearly and does not leave stale loading state
- [ ] **Done** ([line 285](../../../apps/web/src/pages/admin/paired-devices.tsx#L285)) — click path works: disabled state correct, expected API/nav/modal/toast fires, and final row/status/value visibly changes
- [ ] **Done** ([line 285](../../../apps/web/src/pages/admin/paired-devices.tsx#L285)) — failure path works: validation/server error is shown clearly and does not leave stale loading state

## API methods used (3)

- [ ] `api.listPairedDevices` — request goes out, 2xx response, no schema mismatch in browser console
- [ ] `api.mintDevicePairingToken` — request goes out, 2xx response, no schema mismatch in browser console
- [ ] `api.revokePairedDevice` — request goes out, 2xx response, no schema mismatch in browser console

---

_Generated by `scripts/gen_screen_checklist.py`. Re-run after any change to the page to refresh — checkboxes are NOT preserved across regenerations, so commit your ticks before regenerating._
# `admin/settings/ComplianceSettings.tsx` walkthrough

_Source: [`apps/web/src/pages/admin/settings/ComplianceSettings.tsx`](../../../apps/web/src/pages/admin/settings/ComplianceSettings.tsx) (168 lines). Guard: `—`. API methods: 2. useForm: 0. Tables: 0. Modals: 0._

Tick each box that **works as expected** when you walk the page in a browser. Anything you can't tick → file in `../issues.md` with the line number, then fix and come back to tick it. Severity rubric: **P0** blocks core flow, **P1** broken UX with workaround, **P2** cosmetic.

## Page-level

- [ ] Page renders without `console.error`
- [ ] Network tab shows no 4xx/5xx on initial load
- [ ] Desktop viewport has no overlapping text, clipped buttons, or broken table layout
- [ ] Mobile/tablet viewport has no overlapping text, clipped buttons, or unusable controls
- [ ] Permission guard `(none)` redirects unauthorised user to /dashboard
- [ ] Loading skeleton / spinner shown while data loads
- [ ] Empty state visible when there are zero rows
- [ ] Page title in browser tab is correct
- [ ] Breadcrumb / nav highlights this page

### ⚠ Static analysis flags
- `no useRequirePermission guard`

## Tables / lists

### DataTable columns (12)
- [ ] Column **Drug Schedule Badges** (`show_schedule_badges`) renders without `undefined` / `[object Object]`
- [ ] Column **Controlled Substance Warnings** (`show_controlled_warnings`) renders without `undefined` / `[object Object]`
- [ ] Column **Formulary Status** (`show_formulary_status`) renders without `undefined` / `[object Object]`
- [ ] Column **AWaRe Category** (`show_aware_category`) renders without `undefined` / `[object Object]`
- [ ] Column **Drug Scheduling Enforcement** (`enforce_drug_scheduling`) renders without `undefined` / `[object Object]`
- [ ] Column **NDPS Act Compliance** (`enforce_ndps_tracking`) renders without `undefined` / `[object Object]`
- [ ] Column **Formulary Enforcement** (`enforce_formulary`) renders without `undefined` / `[object Object]`
- [ ] Column **Drug Interaction Checks** (`enforce_drug_interactions`) renders without `undefined` / `[object Object]`
- [ ] Column **Antibiotic Stewardship** (`enforce_antibiotic_stewardship`) renders without `undefined` / `[object Object]`
- [ ] Column **LASA Warnings** (`enforce_lasa_warnings`) renders without `undefined` / `[object Object]`
- [ ] Column **Max Dose Validation** (`enforce_max_dose_check`) renders without `undefined` / `[object Object]`
- [ ] Column **Batch Tracking** (`enforce_batch_tracking`) renders without `undefined` / `[object Object]`

## Form inputs (2)

- [ ] **<Switch @ line 127>** (`Switch`, [line 127](../../../apps/web/src/pages/admin/settings/ComplianceSettings.tsx#L127)) — accepts input, default value sensible, persists after refresh
- [ ] **<Switch @ line 155>** (`Switch`, [line 155](../../../apps/web/src/pages/admin/settings/ComplianceSettings.tsx#L155)) — accepts input, default value sensible, persists after refresh

## API methods used (2)

- [ ] `api.getTenantSettings` — request goes out, 2xx response, no schema mismatch in browser console
- [ ] `api.updateTenantSetting` — request goes out, 2xx response, no schema mismatch in browser console

---

_Generated by `scripts/gen_screen_checklist.py`. Re-run after any change to the page to refresh — checkboxes are NOT preserved across regenerations, so commit your ticks before regenerating._
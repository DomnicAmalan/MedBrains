# `audit/BreakGlassReviewTab.tsx` walkthrough

_Source: [`apps/web/src/pages/audit/BreakGlassReviewTab.tsx`](../../../apps/web/src/pages/audit/BreakGlassReviewTab.tsx) (356 lines). Guard: `—`. API methods: 3. useForm: 0. Tables: 1. Modals: 1._

Tick each box that **works as expected** when you walk the page in a browser. Anything you can't tick → file in `../issues.md` with the line number, then fix and come back to tick it. Severity rubric: **P0** blocks core flow, **P1** broken UX with workaround, **P2** cosmetic.

## Page-level

- [ ] Page renders without `console.error`
- [ ] Network tab shows no 4xx/5xx on initial load
- [ ] Desktop viewport has no overlapping text, clipped buttons, or broken table layout
- [ ] Mobile/tablet viewport has no overlapping text, clipped buttons, or unusable controls
- [ ] Permission guard `(none)` redirects unauthorised user to /dashboard
- [ ] Loading skeleton / spinner shown while data loads
- [ ] Empty state visible when there are zero rows
- [ ] Page title in browser tab is correct
- [ ] Breadcrumb / nav highlights this page

### ⚠ Static analysis flags
- `no useRequirePermission guard`

## Tables / lists

### DataTable columns (9)
- [ ] Column **Access** (`lifecycle`) renders without `undefined` / `[object Object]`
- [ ] Column **Review** (`review`) renders without `undefined` / `[object Object]`
- [ ] Column **User** (`user`) renders without `undefined` / `[object Object]`
- [ ] Column **Patient** (`patient`) renders without `undefined` / `[object Object]`
- [ ] Column **Scope** (`scope`) renders without `undefined` / `[object Object]`
- [ ] Column **Requested modules** (`modules`) renders without `undefined` / `[object Object]`
- [ ] Column **PHI reads** (`phi_access_count`) renders without `undefined` / `[object Object]`
- [ ] Column **Window** (`time`) renders without `undefined` / `[object Object]`
- [ ] Column **Actions** (`actions`) renders without `undefined` / `[object Object]`

## Modals / Drawers

### Drawer — _Break-glass evidence_ @ [line 241](../../../apps/web/src/pages/audit/BreakGlassReviewTab.tsx#L241)
- [ ] Opens on trigger
- [ ] All inner tabs activate without error
- [ ] Submit returns 2xx and toast confirms
- [ ] Closes via Esc + Cancel + ✕

## Form inputs (2)

- [ ] **Queue** (`Select`, [line 222](../../../apps/web/src/pages/audit/BreakGlassReviewTab.tsx#L222)) — accepts input, default value sensible, persists after refresh
- [ ] **Supervisor review notes** (`Textarea`, [line 330](../../../apps/web/src/pages/audit/BreakGlassReviewTab.tsx#L330)) — accepts input, default value sensible, persists after refresh

## Buttons / actions (`<Button>`: 2, `<ActionIcon>`: 0, `<Menu.Item>`: 0)

- [ ] **<button @ line 197>** ([line 197](../../../apps/web/src/pages/audit/BreakGlassReviewTab.tsx#L197)) — click path works: disabled state correct, expected API/nav/modal/toast fires, and final row/status/value visibly changes
- [ ] **<button @ line 197>** ([line 197](../../../apps/web/src/pages/audit/BreakGlassReviewTab.tsx#L197)) — failure path works: validation/server error is shown clearly and does not leave stale loading state
- [ ] **<button @ line 338>** ([line 338](../../../apps/web/src/pages/audit/BreakGlassReviewTab.tsx#L338)) — click path works: disabled state correct, expected API/nav/modal/toast fires, and final row/status/value visibly changes
- [ ] **<button @ line 338>** ([line 338](../../../apps/web/src/pages/audit/BreakGlassReviewTab.tsx#L338)) — failure path works: validation/server error is shown clearly and does not leave stale loading state

## API methods used (3)

- [ ] `api.getBreakGlass` — request goes out, 2xx response, no schema mismatch in browser console
- [ ] `api.listBreakGlass` — request goes out, 2xx response, no schema mismatch in browser console
- [ ] `api.reviewBreakGlass` — request goes out, 2xx response, no schema mismatch in browser console

---

_Generated by `scripts/gen_screen_checklist.py`. Re-run after any change to the page to refresh — checkboxes are NOT preserved across regenerations, so commit your ticks before regenerating._
# `care-view.tsx` walkthrough

_Source: [`apps/web/src/pages/care-view.tsx`](../../../apps/web/src/pages/care-view.tsx) (100 lines). Guard: `P.CARE_VIEW.VIEW`. API methods: 1. useForm: 0. Tables: 0. Modals: 0._

Tick each box that **works as expected** when you walk the page in a browser. Anything you can't tick → file in `../issues.md` with the line number, then fix and come back to tick it. Severity rubric: **P0** blocks core flow, **P1** broken UX with workaround, **P2** cosmetic.

## Page-level

- [ ] Page renders without `console.error`
- [ ] Network tab shows no 4xx/5xx on initial load
- [ ] Desktop viewport has no overlapping text, clipped buttons, or broken table layout
- [ ] Mobile/tablet viewport has no overlapping text, clipped buttons, or unusable controls
- [ ] Permission guard `P.CARE_VIEW.VIEW` redirects unauthorised user to /dashboard
- [ ] Loading skeleton / spinner shown while data loads
- [ ] Empty state visible when there are zero rows
- [ ] Page title in browser tab is correct
- [ ] Breadcrumb / nav highlights this page

## Tabs

- [ ] Tab **}>
            Patient Grid** (`grid`) — clicking activates the panel + loads its data without console error
- [ ] Tab **}>
            Patient Grid** (`grid`) — all visible filters/actions inside the tab produce a visible result
- [ ] Tab **}>
            Patient Grid** (`grid`) — leaving and returning preserves or intentionally resets state
- [ ] Tab **}>
              My Tasks** (`tasks`) — clicking activates the panel + loads its data without console error
- [ ] Tab **}>
              My Tasks** (`tasks`) — all visible filters/actions inside the tab produce a visible result
- [ ] Tab **}>
              My Tasks** (`tasks`) — leaving and returning preserves or intentionally resets state
- [ ] Tab **}>
              Handover** (`handover`) — clicking activates the panel + loads its data without console error
- [ ] Tab **}>
              Handover** (`handover`) — all visible filters/actions inside the tab produce a visible result
- [ ] Tab **}>
              Handover** (`handover`) — leaving and returning preserves or intentionally resets state
- [ ] Tab **}>
              Discharge Tracker** (`discharge`) — clicking activates the panel + loads its data without console error
- [ ] Tab **}>
              Discharge Tracker** (`discharge`) — all visible filters/actions inside the tab produce a visible result
- [ ] Tab **}>
              Discharge Tracker** (`discharge`) — leaving and returning preserves or intentionally resets state

## Form inputs (1)

- [ ] **<Select @ line 42>** (`Select`, [line 42](../../../apps/web/src/pages/care-view.tsx#L42)) — accepts input, default value sensible, persists after refresh

## API methods used (1)

- [ ] `api.listWards` — request goes out, 2xx response, no schema mismatch in browser console

---

_Generated by `scripts/gen_screen_checklist.py`. Re-run after any change to the page to refresh — checkboxes are NOT preserved across regenerations, so commit your ticks before regenerating._
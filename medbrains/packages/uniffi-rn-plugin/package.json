{
  "name": "@medbrains/uniffi-rn-plugin",
  "private": true,
  "version": "0.1.0",
  "description": "Expo config plugin that builds the medbrains-edge-rn Rust crate and wires it into the host Expo app's iOS staticlib + Android jniLibs. Wraps uniffi-bindgen-react-native to generate TypeScript Turbo Module bindings.",
  "type": "commonjs",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "require": "./lib/index.js",
      "default": "./lib/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@expo/config-plugins": "^9.0.0"
  },
  "peerDependencies": {
    "expo": ">=51"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.5"
  }
}
